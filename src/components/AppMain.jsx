import BlogCard from "./BlogCards/BlogCards"
export default function AppMain() {
    return (
        <main>
            <section className="mainCards">
                <BlogCard />
            </section>
        </main>
    )
}